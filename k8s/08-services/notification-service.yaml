---
apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-service-config
  namespace: final-proj
data:
  APP_PORT: "8080"
  BROKER_TYPE: "KAFKA"
  PG_HOST: "postgres-notifications"
  PG_PORT: "5432"
  PG_DATABASE: "notificationdb"
  PG_SSLMODE: "disable"
  KAFKA_BROKERS: "kafka:9092"
---
apiVersion: v1
kind: Secret
metadata:
  name: notification-service-secret
  namespace: final-proj
type: Opaque
stringData:
  PG_USER: "notification_user"
  PG_PASSWORD: "notification_password"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: final-proj
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command: ["sh","-c","until nc -z postgres-notifications 5432; do echo waiting...; sleep 2; done"]
          resources:
            requests: { cpu: "10m", memory: "16Mi" }
            limits:   { cpu: "50m", memory: "32Mi" }
      containers:
        - name: notification-service
          image: mblkuta/notificationsvc:finalproj-latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: notification-service-config
            - secretRef:
                name: notification-service-secret
            - secretRef:
                name: shared-secrets
          readinessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests: { cpu: "50m", memory: "64Mi" }
            limits:   { cpu: "200m", memory: "128Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: final-proj
spec:
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
